name: app_pelada_plus
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  micro_core:
    path: packages/core/micro_core

  group:
    path: packages/features/group


dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter